# PDR: プレビュー即時返却 + フル解像度の遅延ダウンロード

- ステータス: 実装済み(PR #13)
- 作成日: 2026-07-05
- 最終更新: 2026-09-26(パスワード保護の撤回、ダウンロード既定形式の JPEG 化、実画像での再計測を反映)
- 対象: `POST /stitch` のレスポンス方式変更(`backend/src/`)、結果表示・ダウンロード動線(`frontend/src/`)
- 関連文書:
  - [PDR-python-backend-modernization.md](./PDR-python-backend-modernization.md)(実装済み。本 PDR はそのレスポンス方式に変更を加える)
  - [PDR-zero-cost-hosting.md](./PDR-zero-cost-hosting.md)(§4 の転送コスト試算を本 PDR の実測値で更新する。ホスティングは 2026-09 に自宅サーバー + Cloudflare Tunnel へ変更済み)

---

## 1. 背景

0 円ホスティング PDR の検討中、実運用のメジャーユースケース(2.8MB JPEG × 6 枚)を想定して実測したところ、2 つの課題が判明した。

1. **合成結果のフル解像度 PNG が非常に大きい**: 実測 73.8MB(20699×3456、71MP)。Cloud Run 運用時、この転送だけで下り無料枠(月 1GB)を月 13〜14 回で使い切る
2. **フル PNG のエンコード自体が重い**: 実測約 1.4 秒。ローカル運用でもレスポンス時間(合計約 2.9 秒)の約半分をこのエンコードが占めている

いずれも「毎回フル解像度を生成・転送する」設計に起因する。実際にはユーザーの多くは画面プレビューを見るだけで、フル解像度を必要とするのは研究データとして保存する場合など一部に限られる。

## 2. 目的

- プレビュー表示に必要な分だけ即座に返し、フル解像度は要求(ダウンロード)時のみ生成・転送する
- **ローカル運用**: レスポンスタイムを短縮する(実測 約 2.9 秒 → 約 1.5 秒、フル PNG エンコードを遅延させることで達成。ネットワーク起因ではない)
- **クラウド運用**: 転送コストを削減し、0 円運用の実質的な余裕を大きく広げる

## 3. 実測データ(2026-07-05、6 枚 × 2.8MB JPEG 入力、Apple Silicon ローカル環境)

| 項目 | 実測値 |
|---|---|
| 入力合計(アップロード) | 17.8MB |
| 合成結果の解像度 | 20699×3456(71MP) |
| フル PNG サイズ | 73.8MB |
| JPEG decode | 267ms |
| stitch 本体 | 1215ms |
| フル PNG encode | 1408ms |
| **合計(現行方式)** | **約 2890ms** |

プレビュー候補の比較(合成結果を縮小 + JPEG 化):

| 解像度 / 品質 | サイズ | フル比 | encode 時間 |
|---|---|---|---|
| 2560px / q85 | 595KB | 1/124 | 4ms |
| **1920px / q80(採用)** | **353KB** | **1/209** | **2ms** |
| 1280px / q80 | 137KB | 1/537 | 1ms |

1920px は一般的な画面表示に十分な解像度で、転送量とのバランスが良いため既定値とする。

計測はテクスチャの多い合成画像によるもので、実際の顕微鏡画像では圧縮率が変わる可能性がある(§10)。

### 3.1 実画像での再計測(2026-09-26)

上表の数値は、実際の顕微鏡画像(2592×1728 JPEG × 6 枚、各 約 2.8MB)では再現しなかった。同じ入力を現行実装と main の実装の両方で合成し、いずれも以下の結果となった(OpenCV 5.0.0、Apple Silicon)。既存の合成結果(`stitched.jpeg`、余白トリミング済み 3211×2949)とも配置・サイズが整合するため、**本節の値を正とする**。上表の 71MP がどの入力・環境によるものかは特定できていない。

| 項目 | 実測値 |
|---|---|
| 合成結果の解像度 | 3500×3238(11MP)。Panorama モードでも 3516×3242 |
| 生画像 1 件のメモリ(キャッシュ保持量) | 約 34MB(3500×3238×3 バイト) |
| 合成(decode + stitch + プレビュー生成) | 1.79 秒(アプリ直接) / 2.75 秒(Docker・nginx 経由、アップロード 16.6MB 込み) |
| プレビュー JPEG(1920px / q80) | 417KB(1920×1776) |
| フル JPEG(q95、既定の DL 形式) | 3.46MB / encode 約 0.03 秒 |
| フル PNG(`format=png`) | 17.7MB / encode 約 0.3 秒 |
| JPEG と PNG の画質差 | PSNR 約 42dB(目視で判別困難) |
| backend コンテナのメモリ | 待機 84MB / 合成時ピーク 約 450MB(`docker stats` を 1〜2 秒間隔で採取) |

## 4. API 契約の変更

Base64 in JSON を避ける既定方針(近代化 PDR)を維持しつつ、プレビューと結果 ID を返す。

### 4.1 `POST /stitch`(変更)

```
200
Content-Type: image/jpeg
X-Result-Id: <uuid>
Body: <プレビューJPEGバイナリ (1920px, q80)>
```

- `X-Result-Id` を JS から読めるよう CORS に `Access-Control-Expose-Headers: X-Result-Id` を追加する
- 422(合成失敗)/ 400(バリデーション)/ 500 のレスポンス形式は変更しない

### 4.2 `GET /stitch/{resultId}/download`(新規)

```
Request:
  Query: format=jpeg | png   (省略時 jpeg)

200 image/jpeg (フル解像度、q95)        (format=jpeg または省略時)
200 image/png  (フル解像度、ロスレス)    (format=png)
400 {"error": "リクエストが不正です: format には jpeg か png を指定してください"}
404 {"error": "結果が見つからないか有効期限切れです"}
```

- フル解像度はアクセスの都度(キャッシュされた生画像から)指定形式にエンコードして返す
- 複数回ダウンロードされても再エンコードで問題ない想定(低頻度のため最適化しない)
- JPEG 画質は `DOWNLOAD_JPEG_QUALITY`(既定 95)で調整できる
- 400 は既存のバリデーションエラー方針(422 は合成失敗専用)に合わせる

**変更履歴**:

- 2026-09-23: パスワード保護(`X-Download-Password` ヘッダー、401 応答)を撤回・削除(§4.3)
- 2026-09-25: 当初は常に PNG を返す設計だったが、既定を JPEG に変更し PNG は `format=png` 指定時のみとした。実画像で JPEG は PNG の約 1/5 のサイズ・encode も約 10 倍速く、画質差は PSNR 約 42dB(§3.1)。普段使いの負荷と転送量を抑え、定量解析など無劣化が必要な場合だけ PNG を選ぶ運用とする

### 4.3 フル解像度ダウンロードのパスワード保護(本番環境のみ)

> **2026-09-23 撤回**: ホスティング先を Cloud Run から自宅サーバー (Cloudflare Tunnel) に変更し、下り転送量の課金がなくなったため、パスワード保護は実装から削除した。以下は検討記録として残す。

**背景**: プレビューは誰でも閲覧できてよいが、フル解像度(研究データとして使える品質)は「身内」のみに限定し、URL を知っただけの野良ユーザーには渡したくない、という要件。

**設計**: `Settings.download_password`(環境変数 `DOWNLOAD_PASSWORD` から読み込み)が**設定されている場合のみ** `X-Download-Password` ヘッダーの一致を要求する。

- ローカル Docker 開発環境: `DOWNLOAD_PASSWORD` を設定しない(既定)→ チェックはスキップされ、開発時に余計な手間がかからない
- Cloud Run 本番環境: `DOWNLOAD_PASSWORD` を Secret Manager 経由の環境変数として設定する → 自動的にパスワード必須になる

「本番かどうか」を判定するフラグを別途持たず、**シークレットが設定されているかどうかだけ**で振る舞いが決まる、12-factor 的なミニマル構成にする。

- 比較は `secrets.compare_digest` によるタイミングセーフ比較を用いる
- パスワードはフロントエンドのビルドに埋め込まない(public リポジトリ・public サイトのため、埋め込むと誰でも閲覧ソースから読める。埋め込んだ時点で保護の意味がなくなる)
- フロントエンドはまずパスワードなしで `GET /download` を試行し、401 が返った場合のみプロンプトで入力を求めて `X-Download-Password` を付けて再試行する(§7)。これによりローカル/本番を問わず同じビルドで動作する

**この方式が守れる範囲(正直な評価)**:

- 「URL を知っただけの野良ユーザーがフル解像度を落とす」ことは防げる(目的どおり)
- ただし本格的な攻撃者(パスワード総当たり等)への耐性は、レート制限を追加実装しない限り弱い。緩和策として**十分に長いランダム文字列**(例: `openssl rand -base64 24`)を使うことを前提とし、覚えやすい単語は避ける
- パスワードを知っている「身内」がさらに他人に教えてしまうリスクは、この方式では原理的に防げない(共有シークレット方式の限界)
- 悪用が実際に観測された場合は、レート制限や Cloud Armor 等の追加を検討する([PDR-zero-cost-hosting.md](./PDR-zero-cost-hosting.md) §7 の「悪用の実観測をトリガーに追加」という既存方針と整合)

## 5. アーキテクチャ

クリーンアーキテクチャの構造を維持し、新しい port を 1 つ追加する。

```
domain/
  ports.py          # + StitchResultCache (Protocol): put(image) -> str, get(id) -> DecodedImage | None
usecase/
  stitch_images.py  # 変更: 成功時にプレビューJPEGを生成し、cache.put()でresultIdを発行
  get_stitch_result.py  # 新規: resultIdからフル画像を取得し、指定形式(既定 JPEG / PNG)でエンコードして返す
infra/
  cache/
    in_memory_result_cache.py  # StitchResultCacheの実装 (TTL付き辞書)
presentation/
  routers/stitch.py  # レスポンスをプレビューJPEG + X-Result-Idヘッダーに変更
  routers/download.py  # 新規: GET /stitch/{resultId}/download?format=jpeg|png
```

- 「結果を後で取得可能にする」はユースケースの責務とし、キャッシュへの登録は `StitchImagesUseCase` 内で行う(domain/usecase は依然として `cv2` に依存しない)
- infra 層に `InMemoryResultCache` を追加。Protocol 化しておくことで、将来複数インスタンス運用になった場合に Redis 等へ差し替え可能にする(現時点ではスコープ外)
- ダウンロード形式は domain の `DownloadFormat`(JPEG / PNG)で表し、形式と Content-Type の対応は presentation 層に閉じる

## 6. キャッシュ設計

| 項目 | 設計 |
|---|---|
| 実装 | プロセス内メモリの辞書(`dict[str, CacheEntry]`)。`CacheEntry = (image, expires_at)` |
| TTL | 10 分 |
| 有効期限切れの扱い | アクセス時に期限チェックして削除する lazy expiry(バックグラウンドスレッド不要) |
| 保持件数上限 | 既定 4 件(`RESULT_CACHE_MAX_ENTRIES` で調整可)。超過時は古いものから追い出す。当初は 71MP ≈ 214MB/件を想定していたが、実画像では約 34MB/件(§3.1)で、4 件でも約 140MB に収まる |
| 複数インスタンス前提 | キャッシュはプロセス内のため、**gunicorn ワーカー 1 本・コンテナ 1 つ**が前提(Dockerfile で `-w 1` に変更済み)。自宅サーバー構成も単一コンテナ。**複数ワーカー・複数ノードにする場合は、共有キャッシュ(Redis・共有ストレージ等)への切り替えが必要**(現時点ではスコープ外) |

## 7. フロントエンド変更

- `ImgSender`: `/stitch` のレスポンス(プレビュー JPEG)を `URL.createObjectURL` で表示。レスポンスヘッダーから `resultId` を読み取り state に保持
- ダウンロード: 現行の直リンクから、クリック時に `GET /api/stitch/{resultId}/download` を fetch する方式に変更(クリック時に初めてフル解像度の転送が発生する)。主ボタン「ダウンロード (JPEG)」、副ボタン「無劣化の PNG でダウンロード (ファイルサイズ大)」(`format=png`)の 2 択
- TTL 切れ(404)の場合は「有効期限が切れました。もう一度合成してください」等のメッセージを表示
- ~~パスワード対応(401 時にプロンプトで入力)~~ — 2026-09-23 撤回(§4.3)

## 8. コスト試算(0 円 PDR §4 の更新)

> **2026-09-26 注記**: 本節は Cloud Run(下り転送量課金あり)を前提とした当時の試算。ホスティングを自宅サーバー + Cloudflare Tunnel に変更したため、転送量による課金は発生しない。また 73.8MB は実画像では再現せず、実測はフル JPEG 3.46MB / フル PNG 17.7MB / プレビュー 417KB(§3.1)。以下は検討記録として残す。

想定シナリオ(月 300 回合成、うち 20%(60 回)がフルダウンロード):

```
プレビュー: 300回 × 353KB ≈ 106MB
フルDL:      60回 × 73.8MB ≈ 4,428MB
合計:            ≈ 4.5GB
無料枠(1GB)超過分: 約3.5GB
概算コスト: 3.5GB × 約15〜18円/GB ≈ 月50〜60円
```

プレビューのみの利用(ダウンロードなし)であれば **月 2,800 回相当まで実質 0 円**。フルダウンロードを毎回行う極端なケースでも、無料枠だけで月 13〜14 回分は賄える。想定規模(月数百回)であれば、実費は 0 円〜数十円/月に収まる見込み。

## 9. 移行計画

| Phase | 内容 | 完了条件 |
|-------|------|----------|
| 1. domain/usecase | `StitchResultCache` port 追加、`StitchImagesUseCase` 改修、キャッシュ保持上限の確定。TDD | 全テスト green |
| 2. infra | `InMemoryResultCache` 実装(TTL・lazy expiry・上限) | ユニットテスト green |
| 3. presentation | `/stitch` レスポンス形式変更、`/stitch/{id}/download?format=jpeg\|png` 新設、CORS の `Expose-Headers` 設定 | 統合テスト green |
| 4. フロントエンド | プレビュー表示・ダウンロード(JPEG / PNG)の動線変更 | `next build` green、実ブラウザ確認 |
| 5. 統合検証 | ローカル Docker で実画像による全フローを実測(§3.1。合成 1.79 秒、狙いの約 1.5 秒に近い) | 実測値取得 — 2026-09-26 完了 |
| ~~6. GCP セットアップ~~ | ~~Secret Manager に `DOWNLOAD_PASSWORD` を作成~~ — 2026-09-23 撤回(Cloud Run を使わずパスワード保護も廃止) | — |
| 7. ドキュメント反映 | 0 円 PDR §4・§7 の記述を本 PDR の内容に合わせて更新 | ドキュメント整合 — 2026-09-26 完了 |

## 10. リスクと留保事項

| リスク / 留保 | 対策・備考 |
|---|---|
| メモリ枯渇(複数の生画像キャッシュが同時に滞留) | 保持件数上限(既定 4 件)+ TTL 10 分で対応。実画像では 1 件約 34MB(§3.1)。入力枚数が多いと 1 件が大きくなるため、低メモリ機では `RESULT_CACHE_MAX_ENTRIES` / `STITCH_MAX_IMAGES` を絞る |
| 単一ワーカー・単一コンテナの前提が将来崩れる | その時点で共有キャッシュ(Redis 等)へ切り替え。現時点ではスコープ外と明記 |
| ダウンロード連打による再エンコードの重複 | 許容(低頻度想定のため最適化しない) |
| プレビュー解像度が研究用途の確認に不十分な可能性 | 1920px は通常十分。必要なら解像度を上げるオプションを将来検討 |
| ~~実測がテクスチャ合成画像に基づく~~ | 2026-09-26 に実画像で再計測済み(§3.1) |
| ~~GCP の下り無料枠(月 1GB)の金額・料率~~ | 自宅サーバー運用への変更により対象外 |
| ~~パスワード総当たり攻撃 / パスワードの又聞き・転送~~ | パスワード保護の撤回により対象外(§4.3) |
| 公開 URL への過剰なリクエスト | 転送費は発生しないが、合成は CPU・メモリを大きく使うため自宅サーバーの負荷になる。身内利用に限るなら Cloudflare Access での認証、または Cloudflare のレート制限ルールを検討 |

## 11. 決定事項

1. プレビュー既定値: 1920px / JPEG q80(353KB)— 2026-07-05 決定
2. フル解像度のエンコードは遅延方式(ダウンロード時に生成)を採用 — 2026-07-05 決定(当時は PNG 固定。形式は決定 6 を参照)
3. キャッシュ TTL: 10 分 — 2026-07-05 決定
4. キャッシュ保持上限件数: 既定 4 件(`RESULT_CACHE_MAX_ENTRIES` で調整可)— 実装時に確定
5. フル解像度ダウンロードは共有パスワードで保護し、本番(Cloud Run)のみ有効化する。ローカル開発では `DOWNLOAD_PASSWORD` 未設定によりチェックをスキップする — 2026-07-05 決定 → **2026-09-23 撤回**(§4.3 参照)
6. フル解像度ダウンロードの既定形式は JPEG(q95)とし、PNG は `format=png` 指定時のみ返す — 2026-09-25 決定
