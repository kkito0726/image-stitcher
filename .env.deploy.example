# docker-compose.deploy.yml 用の設定。`cp .env.deploy.example .env` して値を埋める。
# .env は秘密情報を含むためコミットしないこと (.gitignore 済み)。

# --- 必須 ---
# Cloudflare Zero Trust > Networks > Tunnels で作成したトンネルのトークン
TUNNEL_TOKEN=

# --- ダウンロード ---
# フル解像度ダウンロードは既定で JPEG。PNG (無劣化) は利用者が明示的に選んだ場合のみ
DOWNLOAD_JPEG_QUALITY=95

# --- 機種別のリソース設定 ---
# Raspberry Pi 5 (4GB): メモリが少ないためキャッシュ件数と入力枚数を絞る
# (合成結果はダウンロード用にメモリ保持する。実測で 6 枚合成 1 件 ≈ 34MB、枚数に応じて増える)
BACKEND_CPUS=3.0
BACKEND_MEMORY=2G
RESULT_CACHE_MAX_ENTRIES=2
RESULT_CACHE_TTL_SECONDS=300
STITCH_MAX_IMAGES=15

# 1 リクエストの画像の合計画素数の上限 (既定 4000 万画素)。合成のピークメモリは実測で
# 重なりの大きい実写 6 枚 (2592x1728) で約 15〜18 バイト/画素、重なりの少ない最悪ケースで
# 約 30 バイト/画素なので、BACKEND_MEMORY 2G ではこの程度に抑える。枚数上限とは別にかかり、
# 2592x1728 (約 450 万画素) の画像なら 8 枚まで (上の STITCH_MAX_IMAGES=15 より先に効く)。
# 1 枚あたりの上限は Dockerfile の OPENCV_IO_MAX_IMAGE_PIXELS (4000 万画素)
# STITCH_MAX_TOTAL_PIXELS=40000000

# 上位機種での設定値の決め方と機種別の推奨値は docs/deploy-resource-tuning.md を参照
# Intel N100 (16GB) の場合は上をコメントアウトして以下を使う
# BACKEND_CPUS=3.5
# BACKEND_MEMORY=8G
# RESULT_CACHE_MAX_ENTRIES=4
# RESULT_CACHE_TTL_SECONDS=600
# STITCH_MAX_IMAGES=30
# STITCH_MAX_TOTAL_PIXELS=160000000
